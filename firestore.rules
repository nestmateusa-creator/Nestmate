rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all users to read and write all documents
    // This is for development/testing - should be more restrictive in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
